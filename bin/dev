#!/usr/bin/env sh

if ! gem list foreman -i --silent; then
  echo "Installing foreman..."
  gem install foreman
fi

echo "Running db:setup to run pending migrations (or initialize the DB if this is the first time)"
exec rails db:setup

exec foreman start -f Procfile.dev "$@"
