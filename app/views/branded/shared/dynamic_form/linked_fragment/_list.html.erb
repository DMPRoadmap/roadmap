<% obj_list.each do |obj| %>
  <tr>
    <td><%= obj.to_s %></td>
    <td class="actions">
      <%= link_to new_edit_linked_fragment_madmp_fragments_url(
            nil, 
            :fragment_id => obj.id, 
            :parent_id => parent_id, 
            :classname => classname
        ),  {
            :remote => true, 
            'data-toggle' =>  "modal", 
            'data-target' => '#modal-window'
        }  do %>
        <%= _('Edit') %>
      <% end %>
      <span class="delete" 
        aria-hidden="true"
        data-confirm-message="<%= d_('dmpopidor', 'Do you want to delete this %{classname}?') % { classname: classname } %>"
        data-url="<%= madmp_fragment_path(obj.id) %>"><%= _('Delete') %></span>
    </td>
  </tr>
<% end %>