<%#
  DMPTool customization overview:
  ------------------------------------------
  1. Removed org phone/email contacts
  2. increased height oof google map
 %>
 <!-- new_right template -->
 <!-- new_right template -->
 <address>
   <strong><%= Rails.configuration.x.organisation.name %></strong><br>
  <%= sanitize(Rails.configuration.x.organisation.address.values.compact.join("<br>")) %>
 </address>

 <%
 link=Rails.configuration.x.organisation.google_maps_link
 if link.present? %>
   <iframe
     width="90%"
     height="350"
     frameborder="0"
     scrolling="no"
     marginheight="0"
     marginwidth="0"
     src="<%= link %>">
   </iframe>
 <% end %>
